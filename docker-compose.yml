services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    volumes:
      - app-data:/app/data
    environment:
      - PORT=4000
      - WA_PROXY=${WA_PROXY:-}

volumes:
  app-data:
